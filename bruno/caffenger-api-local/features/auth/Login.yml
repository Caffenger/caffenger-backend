info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:4000/auth/login
  body:
    type: json
    data: |-
      {
        "email": "mih.mare@gmail.com",
        "password": "password1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
