info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:4000/auth/register
  body:
    type: json
    data: |-
      {
        "name": "Tester",
        "email": "test@test.com",
        "password": "testing123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
