info:
  name: Delete Floor
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:4000/cafe/{{cafeId}}/floor/{{floorId}}
  auth:
    type: bearer
    token: { { authToken } }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
