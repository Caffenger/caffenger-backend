info:
  name: Get Cafe Floors
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:4000/cafe/{{cafeId}}/floor
  auth:
    type: bearer
    token: { { authToken } }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
